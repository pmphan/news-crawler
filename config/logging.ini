[loggers]
keys=root

[handlers]
keys=console

[formatters]
keys=standard

[logger_root]
level=DEBUG
handlers=console
propagate=1

[handler_console]
class=StreamHandler
formatter=standard
args=(sys.stdout,)

[formatter_standard]
format=%(asctime)s [%(name)s] %(levelname)s: %(message)s
